### Why?
- POC Proxmox CAPI in a mixed os/multi os env. 
  - If fails test RKE2(ControlP and Boostrap API) + CAPMOX

### Windows TODO: 
- Create Windows Templates and Vars: 
  - kubeadm, boostrap, and md class
  - custom ntp
- Create Imagebuild Artifacts
  - json, xml
  - ansible role to install csi_proxy

### Test:
- using  cluster label: cluster.x-k8s.io/proxmox-cluster-cni: calico/cilium
  - removes need for helm, but research needed into values supported
  - https://github.com/ionos-cloud/cluster-api-provider-proxmox/blob/v1alpha2/examples/cluster-calico.yaml
